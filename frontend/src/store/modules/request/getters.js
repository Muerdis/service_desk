const requests = state => state.requests;
const requestTypes = state => state.requestTypes;
const requestReasons = state => state.requestReasons;
const reservations = state => state.reservations;
const maintenances = state => state.maintenances;

export default {
  requests,
  requestTypes,
  requestReasons,
  reservations,
  maintenances,
};
